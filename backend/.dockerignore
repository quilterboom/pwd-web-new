# 构建镜像时排除以下文件，避免把导出的镜像 tar / 测试脚本 / 本地 venv / 开发数据打进镜像或拖慢构建上下文
offline/*.tar
offline/smoke_entry.py
offline/smoke_hybrid.py
__pycache__/
*.pyc
*.pyo
*.log
.workbuddy/
.venv/
data/
.secret_key
.smoke_*.py

# 前端：frontend/dist 为「构建机预编译」的静态产物，必须打进镜像；
#       node_modules / .vite 是本地依赖与缓存，无需打进上下文。
frontend/node_modules
frontend/.vite
