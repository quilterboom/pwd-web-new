# 2026-07-14 工作日志

## 重新构建镜像（用户更新代码后）
- 触发：用户提交代码（`cdcb3a0 新增批量删除`、`cb96541 优化功能`、`d7a0331 修改数据库并发` 等）后要求「重新构建 docker 镜像」。
- 构建：`bash backend/offline/build_image.sh`（npm install → vite build → docker build --no-cache --platform linux/amd64 → docker save）。后台任务完成约 2m51s。
  - 前端：36 modules transformed，产物 `assets/index-CHiLxpFF.js`(141.08 kB)、`assets/index-BCPWItLH.css`(15.55 kB)。
  - 镜像：`password-manager:latest` = `0e36efcb89fd27f47069fe5fbc08524560fa91e4edf616a25a030cdcdeabddc7`（219MB，amd64）。
  - tar：`backend/offline/password_manager_image.tar`（216M）已更新。
- 冒烟验证（临时容器 `pm-smoke` @ 9019，干净卷，ADMIN_PASSWORD=SmokeTest!2026）：
  - `GET /` → 200，引用新 bundle `assets/index-CHiLxpFF.js`；JS 资源 200（147857 B）。
  - 真实功能端点（无认证）均 401（auth 门禁生效，非 500）：`/api/keys/status`、`/api/groups/mine`、`/api/orgkeys`、`/api/admin/groups`、`/api/admin/users`。
  - 注：早期误测 `/api/groups`、`/api/keys`（404）系路径写错——真实分组端点为 `/api/groups/mine` 与 `/api/admin/groups`，密钥端点为 `/api/orgkeys`+`/api/keys/status`；404 系预期，非 bug。
  - 容器启动无 import/启动期 500（确认用户新提交 batch-delete / key-management / DB 并发修复可正常加载）。验证后 `docker rm -f pm-smoke` + `rm -rf .smoke_data` 清理。
- 部署命令：`docker load -i backend/offline/password_manager_image.tar && docker compose up -d`。
- 最终交付物：镜像 `0e36efcb89fd` + `password_manager_image.tar`（216M）。
