# 2026-07-10 工作记录

## 重新打包镜像（按最新 HEAD 重建）
- 触发：用户说"重新打个包"。检查发现 dev 容器 `pm-test2`（绑定宿主）已服务 `app.js?v=11`，但已提交镜像/旧 tar 仍是 `?v=6`——源码在我上次打包后又有多次提交（`8949244 修改应用使用报错` 等），镜像已落后。
- 做法：增量 `docker commit`（沿用已验证的 commit 参数，未用 `--entrypoint sleep` 以避免捕获错误 ENTRYPOINT）：
  - 起 `pm-build` 临时容器（`sleep 600` 作 CMD）。
  - `rm -rf /app/app` → `docker cp backend/app` + `run.py` + `requirements.txt` → 清 `*.pyc`/`__pycache__`。
  - 容器内 `importlib` 校验 10 个模块全部 OK；`index.html` 已是 `?v=11`；功能标记（sm3Hex/openUserBatch/private_passphrase/setFormEditLocked）齐全。
  - `docker commit --change 'CMD ["python","run.py"]' --change 'EXPOSE 9010' --change 'VOLUME ["/app/data"]' pm-build password-manager:latest`。
- 冒烟：`pm-smoke` 全新容器（9099→9010）启动正常；SCRAM 登录 200、keys/status 200(gpg+sm2)、xlsx 模板 200(5782B)、首页 `?v=11` 正确。
- 导出：`docker save` → `backend/offline/password_manager_image.tar`（**231MB**，镜像展开 235MB）。清理 `pm-build`/`pm-smoke`。
- 依赖无新增（requirements 未变，openpyxl 等镜像内均 OK）。

## 注意
- 每次"重新打包"前务必先 `grep app.js?v=` 对比 dev 容器与镜像版本，确认镜像确实落后再重建；否则会无谓地重新打。
- `backend/node_modules` 仍存在（被 gitignore），但 `docker cp backend/app` 不会把它带进镜像，无影响。

## 修复 5 项问题 + 重新打包 v=12
- 范围：密码导入（新增）、导出仅管理员、弹框关闭逻辑、解密密码只读、明文标签澄清。
- **路由顺序坑（真坑，非理论）**：本 Starlette 版本 `{pid}:int` 会**先匹配路径再校验 int**，非 int 段（如 `template`/`import`）不回退、直接 422（`loc:["path","pid"],input:"template"`）。`/export` 因是 POST 且路径不同才没被挡。`/template`、`/import` 必须定义在 `@router.get("/{pid}")` **之前**。已用脚本把 passwords.py 中 668–1027 行整块（template/import 路由 + 辅助函数）移到 get_one 之前；现顺序：template(369)/import(538) → {pid}(657)。
- **算法校验 bug**：`import_passwords` 错用 `manager.SUPPORTED`（=("gpg","sm2")）校验，导致 `symmetric` 导入必 400。新增模块常量 `VALID_ALGOS = ("symmetric","gpg","sm2")`（passwords.py:32），`import_passwords`(538) 与 `update`(766) 统一用它；`_encrypt_for_create` 本就支持 symmetric。
- **前端**：弹框去掉 backdrop 关闭（仅「取消」/X 按钮）；`unlockEdit` 解密后置 `$("f-entry-password").value=pw` 并 `lockEntryPasswordField()`（disabled+readOnly，查看/修改时该框只读，`saveForm` 仍读 `.value` 作为 entry_password）；`index.html` 标签改为「密码明文（真实密码内容）*」+ hint 区分「解密密码」；导出按钮 `enterApp` 按 `state.isAdmin` 显隐。
- 验证：`e2e_import_test.py` 全过（模板 xlsx/csv 200、非管理员导出 403、管理员导出 400、CSV 导入 2 created、解锁 secret=SecretA!1、xlsx 回环 2 created、清理）。**注意该测试脚本历史有个 2/3-tuple 解包 bug 已修**（`req` 现返回 (status, body)）。
- 重新打包：起 `pm-build`→`rm -rf /app/app`→`docker cp backend/app`→清 pyc→`docker commit --change 'ENTRYPOINT ["python","run.py"]' --change 'CMD []' --change 'EXPOSE 9010' --change 'VOLUME ["/app/data"]'`（沿用正确 ENTRYPOINT 覆盖）→`pm-verify` 全新容器起 uvicorn 正常→`docker save`→`password_manager_image.tar`（**232MB**）。`?v=12`。

## 用户三需求：美化用户编辑弹窗 + 自助改密 + Vue 可行性
- **1. 美化管理员编辑用户弹窗**：`#user-modal` 重构为 `user-card`，新增头像块（`#user-avatar` 渐变方块 + 首字母）、角色徽章（`#user-role-badge`）、分区卡片（账户信息 / 权限与分组）、开关式管理员（`.switch`+`.slider`，`:has(input:checked)` 高亮选择分组）、网格分组勾选（`.checkbox-grid`）。保持原 ID（`u-username`/`u-password`/`u-isadmin`/`u-groups`/`user-error`/`user-cancel`/`user-save`/`user-modal-title`）不变，`openUserAdd/openUserEdit/saveUser` 逻辑零改动；新增 `u-pwd-reveal` 显隐密码。样式加在 `styles.css` 末尾。
- **2. 自助修改登录密码（所有登录用户，含非管理员）**：
  - 后端 `auth.py` 新增 `POST /api/auth/change-password/begin`（登录态，返 salt+nonce；legacy 用户返 `mode:"legacy"` 无 salt/nonce）+ `POST /api/auth/change-password/verify {nonce,proof,new_password | current_password,new_password}`。复用 SCRAM-SM3 校验当前密码（`consume_login_challenge`+`expected_proof`+`compare_digest`），legacy 兜底 `verify_password`；校验通过同时更新 `pw_salt`/`pw_verifier`(SM3)/`hashed_password`(bcrypt) 三件套；新密码≥8 位且 ≠ 当前。导入 `hash_password` 到 auth.py。
  - 前端：顶栏新增「🔑 修改密码」按钮（所有用户可见）；`changepw-modal`（当前/新/确认三框 + 显隐）；`doChangePw` 复刻 `doLogin` 的 SCRAM 计算（salt/nonce hex→bytes→`sm3Bytes` 拼接）。`enterApp` 无需改（按钮常显）。
  - bump `?v=13`。
  - 验证：`e2e_changepw_test.py` 全过（非管理员自助改密 200、新密码登录成功、错误当前密码 401、新密码过短 400、管理员自助改密+还原、清理）。镜像层 `pm-verify`(9014) 用项目内绝对卷路径确认全新库 E2E 也过，再 `docker save`→tar（**232MB**）。
- **3. Vue 重构可行性（分析，未实施）**：当前为 vanilla JS 单文件 `app.js`(~1980 行)+`index.html`+`styles.css`，无构建/依赖、静态托管、`?v=N` 缓存戳，含自研 SM3（SCRAM 登录必须保留）。结论：**可行**——用 Vite + Vue3 SFC 重写，构建产物仍是静态文件，Docker 只需换 `dist/`；Pinia 替代全局 `state`，每弹窗/表格拆组件。但成本不小（~2000 行命令式 DOM 迁移、删除两步确认/导出逐行等复杂交互需逐一保真），且离线部署约束要求构建产物静态化。建议：若后续 UI 迭代频繁可分期（先 pilot 一个弹窗），否则当前方案够用。**本次仅分析，未动代码。**
- 注意：`backend/offline/_verifydata` 是验证容器临时卷，已清理；Git Bash 的 `/tmp` 删不掉 WSL2 Docker 卷，验证容器卷务必用项目内绝对路径。
