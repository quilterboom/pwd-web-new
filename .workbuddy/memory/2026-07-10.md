# 2026-07-10 工作记录

## 重新打包镜像（按最新 HEAD 重建）
- 触发：用户说"重新打个包"。检查发现 dev 容器 `pm-test2`（绑定宿主）已服务 `app.js?v=11`，但已提交镜像/旧 tar 仍是 `?v=6`——源码在我上次打包后又有多次提交（`8949244 修改应用使用报错` 等），镜像已落后。
- 做法：增量 `docker commit`（沿用已验证的 commit 参数，未用 `--entrypoint sleep` 以避免捕获错误 ENTRYPOINT）：
  - 起 `pm-build` 临时容器（`sleep 600` 作 CMD）。
  - `rm -rf /app/app` → `docker cp backend/app` + `run.py` + `requirements.txt` → 清 `*.pyc`/`__pycache__`。
  - 容器内 `importlib` 校验 10 个模块全部 OK；`index.html` 已是 `?v=11`；功能标记（sm3Hex/openUserBatch/private_passphrase/setFormEditLocked）齐全。
  - `docker commit --change 'CMD ["python","run.py"]' --change 'EXPOSE 9010' --change 'VOLUME ["/app/data"]' pm-build password-manager:latest`。
- 冒烟：`pm-smoke` 全新容器（9099→9010）启动正常；SCRAM 登录 200、keys/status 200(gpg+sm2)、xlsx 模板 200(5782B)、首页 `?v=11` 正确。
- 导出：`docker save` → `backend/offline/password_manager_image.tar`（**231MB**，镜像展开 235MB）。清理 `pm-build`/`pm-smoke`。
- 依赖无新增（requirements 未变，openpyxl 等镜像内均 OK）。

## 注意
- 每次"重新打包"前务必先 `grep app.js?v=` 对比 dev 容器与镜像版本，确认镜像确实落后再重建；否则会无谓地重新打。
- `backend/node_modules` 仍存在（被 gitignore），但 `docker cp backend/app` 不会把它带进镜像，无影响。

## 修复 5 项问题 + 重新打包 v=12
- 范围：密码导入（新增）、导出仅管理员、弹框关闭逻辑、解密密码只读、明文标签澄清。
- **路由顺序坑（真坑，非理论）**：本 Starlette 版本 `{pid}:int` 会**先匹配路径再校验 int**，非 int 段（如 `template`/`import`）不回退、直接 422（`loc:["path","pid"],input:"template"`）。`/export` 因是 POST 且路径不同才没被挡。`/template`、`/import` 必须定义在 `@router.get("/{pid}")` **之前**。已用脚本把 passwords.py 中 668–1027 行整块（template/import 路由 + 辅助函数）移到 get_one 之前；现顺序：template(369)/import(538) → {pid}(657)。
- **算法校验 bug**：`import_passwords` 错用 `manager.SUPPORTED`（=("gpg","sm2")）校验，导致 `symmetric` 导入必 400。新增模块常量 `VALID_ALGOS = ("symmetric","gpg","sm2")`（passwords.py:32），`import_passwords`(538) 与 `update`(766) 统一用它；`_encrypt_for_create` 本就支持 symmetric。
- **前端**：弹框去掉 backdrop 关闭（仅「取消」/X 按钮）；`unlockEdit` 解密后置 `$("f-entry-password").value=pw` 并 `lockEntryPasswordField()`（disabled+readOnly，查看/修改时该框只读，`saveForm` 仍读 `.value` 作为 entry_password）；`index.html` 标签改为「密码明文（真实密码内容）*」+ hint 区分「解密密码」；导出按钮 `enterApp` 按 `state.isAdmin` 显隐。
- 验证：`e2e_import_test.py` 全过（模板 xlsx/csv 200、非管理员导出 403、管理员导出 400、CSV 导入 2 created、解锁 secret=SecretA!1、xlsx 回环 2 created、清理）。**注意该测试脚本历史有个 2/3-tuple 解包 bug 已修**（`req` 现返回 (status, body)）。
- 重新打包：起 `pm-build`→`rm -rf /app/app`→`docker cp backend/app`→清 pyc→`docker commit --change 'ENTRYPOINT ["python","run.py"]' --change 'CMD []' --change 'EXPOSE 9010' --change 'VOLUME ["/app/data"]'`（沿用正确 ENTRYPOINT 覆盖）→`pm-verify` 全新容器起 uvicorn 正常→`docker save`→`password_manager_image.tar`（**232MB**）。`?v=12`。

## 用户三需求：美化用户编辑弹窗 + 自助改密 + Vue 可行性
- **1. 美化管理员编辑用户弹窗**：`#user-modal` 重构为 `user-card`，新增头像块（`#user-avatar` 渐变方块 + 首字母）、角色徽章（`#user-role-badge`）、分区卡片（账户信息 / 权限与分组）、开关式管理员（`.switch`+`.slider`，`:has(input:checked)` 高亮选择分组）、网格分组勾选（`.checkbox-grid`）。保持原 ID（`u-username`/`u-password`/`u-isadmin`/`u-groups`/`user-error`/`user-cancel`/`user-save`/`user-modal-title`）不变，`openUserAdd/openUserEdit/saveUser` 逻辑零改动；新增 `u-pwd-reveal` 显隐密码。样式加在 `styles.css` 末尾。
- **2. 自助修改登录密码（所有登录用户，含非管理员）**：
  - 后端 `auth.py` 新增 `POST /api/auth/change-password/begin`（登录态，返 salt+nonce；legacy 用户返 `mode:"legacy"` 无 salt/nonce）+ `POST /api/auth/change-password/verify {nonce,proof,new_password | current_password,new_password}`。复用 SCRAM-SM3 校验当前密码（`consume_login_challenge`+`expected_proof`+`compare_digest`），legacy 兜底 `verify_password`；校验通过同时更新 `pw_salt`/`pw_verifier`(SM3)/`hashed_password`(bcrypt) 三件套；新密码≥8 位且 ≠ 当前。导入 `hash_password` 到 auth.py。
  - 前端：顶栏新增「🔑 修改密码」按钮（所有用户可见）；`changepw-modal`（当前/新/确认三框 + 显隐）；`doChangePw` 复刻 `doLogin` 的 SCRAM 计算（salt/nonce hex→bytes→`sm3Bytes` 拼接）。`enterApp` 无需改（按钮常显）。
  - bump `?v=13`。
  - 验证：`e2e_changepw_test.py` 全过（非管理员自助改密 200、新密码登录成功、错误当前密码 401、新密码过短 400、管理员自助改密+还原、清理）。镜像层 `pm-verify`(9014) 用项目内绝对卷路径确认全新库 E2E 也过，再 `docker save`→tar（**232MB**）。
- **3. Vue 重构可行性（分析，未实施）**：当前为 vanilla JS 单文件 `app.js`(~1980 行)+`index.html`+`styles.css`，无构建/依赖、静态托管、`?v=N` 缓存戳，含自研 SM3（SCRAM 登录必须保留）。结论：**可行**——用 Vite + Vue3 SFC 重写，构建产物仍是静态文件，Docker 只需换 `dist/`；Pinia 替代全局 `state`，每弹窗/表格拆组件。但成本不小（~2000 行命令式 DOM 迁移、删除两步确认/导出逐行等复杂交互需逐一保真），且离线部署约束要求构建产物静态化。建议：若后续 UI 迭代频繁可分期（先 pilot 一个弹窗），否则当前方案够用。**本次仅分析，未动代码。**
- 注意：`backend/offline/_verifydata` 是验证容器临时卷，已清理；Git Bash 的 `/tmp` 删不掉 WSL2 Docker 卷，验证容器卷务必用项目内绝对路径。

## Vue3 重构前端（实施完成）
- **目标**：把 vanilla `app.js`(~1980 行)+`index.html`+`styles.css` 重写为 Vue3 + Vite SFC，业务 1:1 保真（SCRAM-SM3 登录含 409 明文回退、密码 CRUD+解密锁、xlsx 导入导出、OrgKey 库、管理员面板、审计、用户批量导入、两步删除确认、自助改密）。
- **结构**：`backend/frontend/`（vite `base:'./'`、无 vue-router 用手动 `state.currentTab` 切 tab、dev proxy `/api→:9010`）；`src/` 拆 `api/{http,auth}.js`、`crypto/sm3.js`(纯 JS SM3+`scramProof`)、`store.js`(中央 reactive state)、`utils.js`(esc/fmtTime/algoBadge/humanizeComment)、`components/*`(Login/PasswordPanel/KeyPanel/各 Modal)。`styles.css` 原样搬入。CSP 保持 `script-src 'self'`（全部同源 bundled）。
- **Dockerfile 改为单阶段**：前端在构建机 `npm run build` 预编译 `frontend/dist`（静态资源与架构无关），Dockerfile 直接 `COPY frontend/dist ./app/static`（WORKDIR=/app → 即 `/app/app/static`，正是 `main.py` 的 `STATIC_DIR`）。`.dockerignore` 不再排除 `frontend/dist`，`build_image.sh` 已加 `cd backend/frontend && npm install && npm run build` 预构建步骤。运行时不变（仍只跑 `python run.py`，9010）。
- **致命坑（已踩已修）**：Dockerfile `WORKDIR /app` 下相对 COPY 目标会多嵌套一层。初版写 `COPY frontend/dist ./app/app/static` → 实际落到 `/app/app/app/static`（应用读 `/app/app/static`）。**正确写法 `COPY frontend/dist ./app/static`**。多阶段 `COPY --from` 在 Apple Silicon+QEMU 下还出现过层物化异常（老前端缓存进了 `/app/app/static` 纯属歪打正着），故放弃多阶段、改用宿主机预构建 + 单阶段 `COPY`，目标路径显式可控。
- **回归验证（容器内 pm-verify 对构建镜像实跑）全过**：smoke_migration 10/10、smoke_export(xlsx+解密失败拦截) 12/12、smoke_security(CSP/DENY/CORS/重放拒/限速429) 全过、smoke_keyimport(SM2 passphrase 形参一致性·曾 500→现解密读回 'SM2-中文-secret'、受口令 GPG 导入 400+带口令 200) 全过、smoke_keydelete 全过。前端 bundle 含全部关键 API 路径 + SCRAM 符号 + 409 回退 + xlsx；groups 经 `/api/auth/me` 的 `groups` 字段加载（等价 `/api/groups/mine`，无功能缺口）。
- **交付**：`docker save` → `backend/offline/password_manager_image.tar`（72MB 压缩 / 镜像 318MB，`docker load` 已验证完整含 Vue 产物）。`docker-compose.yml` 注释已同步（预构建提示、约 320MB）。

## 用户验收微调（问题清单逐条修）
- **问题1：密码页面非管理员不展示导出按钮**。`PasswordPanel.vue` 导出 `<button id="export-btn">` 加 `v-if="state.isAdmin"`（store.isAdmin 来自 `/api/auth/me` 的 `is_admin`）；`openExport()` 再加 `if(!state.isAdmin)` 防御（后端 `/export` 本身已 `Depends(require_admin)` 拦截，前后端一致）。`npm run build`(产物 `index-mSYKdNjq.js`)→`docker build`(Python 层缓存，仅 COPY 重跑，1s)→`docker save` 重新导出 tar(72MB)。
- 注：用户提及"几个问题"，本批只收到第 1 条；后续问题继续按「改源码→`npm run build`→`docker build`→`docker save`」流程处理。

## 验收微调（第二批·问题清单）
- **问题（切换 tab 不请求数据）**：根因 App.vue 用 `v-show` 常驻挂载两面板，`onMounted` 只触发一次；原 `switchTab('pw')` 不重载、`enterApp` 默认 pw 标签不加载密钥库，导致切换标签不再向服务端请求（首屏靠 KeyPanel onMounted 一次加载，之后切回不刷新）。
  - 修复：`enterApp` 改为同时 `loadEntries()+loadOrgKeys()`；`switchTab` 两方向都重载目标标签数据（切「密钥库」`loadOrgKeys()`、切「密码」`loadEntries()`）。移除 KeyPanel 冗余的 `onMounted(() => loadOrgKeys())`（数据加载统一由 enterApp+switchTab 驱动，避免启动双请求）。
  - 产物 `index-CGAkcfCi.js`；`docker build`(Python 层缓存,2s)→`docker save` 重导出 tar(72MB)。

## 验收微调（第三批·问题清单）
- **问题1：弹框点击背景即关闭**。`v-show` 各弹框 `.modal` 都带 `@click.self="emit('close')"`（点背景关闭）。修复：13 个弹框（含 DeleteConfirmModal 两步）全部移除 `@click.self`；`styles.css` 给 `.modal-card` 加 `position:relative`、新增 `.modal-close`（绝对定位右上角 ✕，hover 高亮）、`.modal-card h2` 加 `padding-right:36px` 防标题与 ✕ 重叠；每个 `.modal-card` 首行插入 `<button class="modal-close" @click="emit('close')">✕</button>`（DeleteConfirm 用 `cancel`）。现在仅「取消/关闭」按钮或右上角 ✕ 可关。
- **问题2：密钥库导出/删除仅管理员**。（1）前端 `KeyPanel.vue`：操作列表头 `<th v-if="state.isAdmin">`、操作单元格 `<td v-if="state.isAdmin">`、空态行 `:colspan` 动态 8/7——非管理员不展示操作列。（2）后端 `app/routers/keys.py`：`export_orgkey`(225) 与 `delete_orgkey`(270) 的 `Depends(get_current_user)` 改为 `Depends(require_admin)`（导入 require_admin），真正服务端拦截。
  - 运行时验证（pm-v2 容器）：非管理员 viewer 访问密钥导出=403、删除=403；管理员同端点=404（通过门禁、无该密钥），证明门禁生效。
  - 产物 `index-C1J8K-k9.js`/`Xl4nxA9W.css`；`docker build`(1s,带新 keys.py)→`docker save` 重导出 tar(72MB)。

## 验收微调（第四批·管理员按分组指定 + 分组弹框成员）
- **需求1：管理员改为可按分组指定（不再是"全部分组管理员"）**。
  - 后端：`models.py` 新增 `user_admin_groups` 关联表（user_id/group_id）；`deps.get_user_groups` 重写——普通用户仅见所属分组；**超级管理员（未指定管理分组）见全部分组**；**分组管理员仅见「所属分组 ∪ 管理的分组」**。密码/密钥/导出/审计均走 `get_user_groups` 这一道闸 → 数据可见性自动按指定分组隔离（`keys.py list_orgkeys` 去掉 `if not user.is_admin` 特例，永远按可见分组过滤）。
  - `admin.py`：`UserCreate/UserUpdate` 增 `admin_group_ids`；`create/update_user` 写 `user_admin_groups`；分组管理员**不能**创建/修改其他管理员、不能管理范围外分组（`_admin_scope`、`_check_can_manage_user`、`_check_group_manageable` 防御）、`list_users/list_groups/audit` 按范围过滤；分组管理员新建分组自动纳入其管理范围（`group_ids` 含新建组）。
  - `/api/auth/me` 新增 `is_global_admin`（=`is_admin and not admin_groups`）。
  - 前端：`store.js` 加 `state.isGlobalAdmin`（refreshMe 取 `is_global_admin`）；`UserFormModal.vue` 管理员开关仅 `isGlobalAdmin` 可见，管理员下新增「管理的分组」多选（提交 `admin_group_ids`，非全局管理员用户不可见该控件）；`AdminModal.vue` 用户表新增 `admin_scope` 副行展示「管理分组 / 全部分组 / 普通用户」，并 `loadUsers` 时把用户列表写入 `state.users`（修问题3根因）。
- **需求2：新增分组框不展示成员**。`GroupFormModal.vue`：`isAdd` 时不渲染成员区（`v-if="!isAdd"`）；编辑态才显示成员多选。
- **需求3：编辑分组框成员为空（根因修复）**。`state.users` 此前从未被赋值（AdminModal 用本地 `users` ref），导致编辑分组成员列表为空。修复：`AdminModal.loadUsers` 同时 `state.users = data`；`GroupFormModal` 编辑态成员列表合并「`state.users` ∪ 分组现有成员（按 id 去重）」，避免越权场景下误删不可见成员。
- **验证**：后端 TestClient 逻辑测试 19/19；容器内 E2E（`e2e_admin_scope.py`，pm-admin-e2e:9015，ADMIN_PASSWORD=TestPass!2026）12/12 PASS（is_global_admin、scoped admin 仅见 A、范围外删组=403、建 admin=403、分组成员回显等）。前端 `npm run build` 通过。
- **交付**：`docker build --no-cache`→`docker save`→`backend/offline/password_manager_image.tar`（72MB）。

## 验收微调（第五批·合并管理员分组选择器）
- **需求：编辑用户页「权限与分组」里「管理的分组」与「所属分组」两个选择器合并为一个**。
  - `UserFormModal.vue`：删除独立「管理的分组」勾选区，只保留一个「分组」选择器（`groupIds`）；管理员勾选的分组**同时**作为所属分组与管理分组提交（`save` 中 `is_admin` 为真时 `payload.admin_group_ids = groupIds`）；不勾选任何分组 = 超级管理员。编辑已有分组管理员时 `onMounted` 预填 `groups ∪ admin_groups` 并集，避免旧数据丢失。
  - 后端无需改动（`admin.py` 本就接收 `group_ids`+`admin_group_ids`，同值即可）。`state.isGlobalAdmin` 控制管理员开关可见性不变。
  - 坑：测试断言漏算「默认分组」导致一度误报；清洁容器后 `e2e_merge_group.py` 9/9 PASS（合并建分组管理员=A、超管见全部分组、改 A→A+B 生效、`mgr` 同时是 A 成员）。
  - 前端 `npm run build` 通过（`index-Duk4f_Ot.js`）；`docker build --no-cache`→`docker save` 重导出 tar（72MB）。

## 验收微调（第六批·管理员三页搜索框）
- **需求：系统管理的「用户 / 分组 / 审计」三个页都加搜索框**。
  - `AdminModal.vue`：新增 `userSearch`/`groupSearch`/`auditSearch` 三个 ref 与对应 `computed`（`filteredUsers`/`filteredGroups`/`filteredAudit`），均为**前端实时过滤**（数据已 load 进前端，无需后端改接口）。
    - 用户：按 用户名 / 所属分组 / 管理分组 模糊匹配。
    - 分组：按 分组名 / 成员 模糊匹配。
    - 审计：按 账号 / 标题 / 分组 / 操作人 / 说明 模糊匹配（说明经 `humanizeComment`）。
  - 每个区块 toolbar 加 `<input class="search-input">`，表格 v-for 改绑 filtered 数组，空态文案改为「无匹配…」。
  - `styles.css`：`.toolbar .search-input` 加 `flex:1;min-width:160px;max-width:320px`（复用 `#search-input` 样式）。
  - 纯前端改动，无后端变更；前端 `npm run build` 通过（`index-CtJ4JnUl.js`），产物含三处 placeholder；`docker build --no-cache`→`docker save` 重导出 tar（72MB）。
