# 2026-07-15 工作日志

## 为离线密码管理器启用 HTTPS（自签名 + HTTP 跳转）
- 需求：用户要求访问网址用 HTTPS。内网离线环境，选择：自签名证书（IP+域名都进 SAN）+ 保持 9010 作 HTTPS 端口 + 明文 HTTP 自动 301/307 跳 HTTPS。
- 关键前提（已确认）：
  - `run.py` 原生支持 TLS（设 `SSL_CERTFILE`/`SSL_KEYFILE` 即走 uvicorn TLS），**无需改前端**（前端 `api()` 用同源相对路径 `fetch('/api/...')`，无混合内容问题）。
  - 前端产物 `index-CHiLxpFF.js` 本轮未变（只改了 run.py/main.py/compose）。
- 改动：
  - `backend/run.py`：SSL 启用时除 uvicorn TLS 外，另起后台线程 `ThreadingHTTPServer` 明文服务（端口 `HTTP_PORT` 默认 9080），对所有方法 307 跳 `https://<host>:<PORT>`；`<host>` 取自请求 Host 头（去端口），故 IP/域名都适用。`SSL_REDIRECT`（默认1）可关。
  - `backend/app/main.py`：`security_headers` 中间件新增**可选 HSTS**（仅 `SSL_CERTFILE` 设置且 `HSTS=1` 时下发 `max-age=31536000; includeSubDomains`），默认关，避免自签名轮换被锁死。
  - `backend/offline/gen_cert.sh`：openssl 一键生成自签名证书，SAN 含 域名/localhost/IP/127.0.0.1（IP、域名作参数），输出 `backend/ssl/`（server.crt/server.key），有效期 10 年。
  - `docker-compose.yml`：挂载 `./backend/ssl:/app/ssl:ro`；新增 `SSL_CERTFILE/SSL_KEYFILE/SSL_REDIRECT/HTTP_PORT/HSTS` 环境变量；明文端口 `${HTTP_PORT:-9080}:9080`；**未设 SSL 变量时完全向后兼容明文 HTTP**。
  - `backend/.env.example`：补充 SSL 变量说明。`Dockerfile` EXPOSE 改为 `9010 9080`。
- 重建：`bash backend/offline/build_image.sh`（仅重拷 run.py 层，pip 缓存命中，快）。新镜像 `password-manager:latest` = `d726caf223b8`（219MB，10:08）；`backend/offline/password_manager_image.tar` 更新（216M）。
- 冒烟验证（临时容器 `pm-ssl`，挂载测试证书，HSTS=1）：
  - `https://127.0.0.1:9443/` → 200，引用 `index-CHiLxpFF.js`，HSTS 头 `max-age=31536000; includeSubDomains` 出现。
  - `http://127.0.0.1:9081/` → 307，`Location: https://127.0.0.1:9010/`（主机名自动识别）。
  - `https://.../api/passwords|/api/auth/me|/api/keys/status` → 均 401（TLS 下鉴权门禁正常）。
  - 验证后 `docker rm -f pm-ssl` + `rm -rf .ssl_test .ssl_data` 清理。
- 部署给用户的步骤（在离线服务器）：`bash backend/offline/gen_cert.sh <IP> <域名>` → `.env` 设 `SSL_CERTFILE=/app/ssl/server.crt`、`SSL_KEYFILE=/app/ssl/server.key` → `docker compose up -d` → 浏览器访问 `https://<IP>:9010`（自签名需手动信任）。
- 交付物：镜像 `d726caf223b8` + `password_manager_image.tar`（216M）。

## 登录报 401 排查（用户反馈 admin 登录 401）
- 结论：**登录代码本身无 bug**。用前端原生 `sm3.js` 在干净数据卷 + 正确密码做端到端 SCRAM 登录，`login/begin`→`login/verify` 全程 200 并拿到 JWT；前后端 SM3 字节级对齐，HTTPS 改造不影响登录。
- 401 根因只可能是「密码错」或「旧数据卷账号凭据不兼容」（如旧账号只有 bcrypt `hashed_password` 无 `pw_verifier`，触发 409 回退 `/login` 明文路径）。
- 最经典坑：**升级镜像挂了旧数据卷 + 改了 `ADMIN_PASSWORD`，但 seed 只在用户不存在时跑一次**，已存在 admin 的密码不变 → 用旧密码登录或强重置。
- 强重置 admin 密码命令（容器内，同时写 SCRAM `pw_verifier`+bcrypt `hashed_password`，两种登录路径都通）：
  ```bash
  docker exec -e NEW_PW='新密码' password-manager python - <<'PY'
  import os, secrets
  from app.db import SessionLocal
  from app.security import hash_password, derive_password_verifier
  from app.models import User
  db = SessionLocal()
  u = db.query(User).filter_by(username='admin').first()
  if not u:
      print('admin 不存在（全新部署 seed 未生效，需另查）'); raise SystemExit
  new = os.environ['NEW_PW']
  u.pw_salt = secrets.token_bytes(16).hex()
  u.pw_verifier = derive_password_verifier(new, u.pw_salt)
  u.hashed_password = hash_password(new)
  db.commit()
  print('OK: admin 密码已重置，请重新登录')
  PY
  ```
- 只读诊断（看 admin 是否存在 + 凭据格式）：`docker exec password-manager python -c "from app.db import SessionLocal; from app.models import User; [print(u.username,'admin=',u.is_admin,'SCRAM=',bool(u.pw_verifier),'bcrypt=',bool(u.hashed_password)) for u in SessionLocal().query(User).all()]"`

## 新增「自助注册」功能（开放模型 + 总开关，默认关）
- 需求：用户要求加「注册用户」功能。安全决策：开放自助注册 + 总开关 `ALLOW_REGISTRATION`（默认关）+ 自动加入默认分组 `REGISTER_DEFAULT_GROUP`（默认「默认分组」），注册只建**普通用户**（is_admin=False），由管理员后台分配权限/分组。
- 改动：
  - `backend/app/routers/auth.py`：新增公开端点 `POST /api/auth/register`（入参 `username`/`password`/`confirm_password`）；受 `ALLOW_REGISTRATION` 总开关控制（默认关→403「当前系统未开放注册」）；**独立注册限速** `_register_rate_limit`（默认 1h 内 10 次/IP，与登录限速分开）；校验 用户名≥3位、密码≥8位、两次一致、用户名唯一；创建用户复用 `_seed_login_material` 写 SCRAM(`pw_salt`/`pw_verifier`)+bcrypt(`hashed_password`) 双凭据；自动 find-or-create `REGISTER_DEFAULT_GROUP` 并 `_link_user_group` 加入。
  - `backend/app/config.py`：新增 `ALLOW_REGISTRATION`（默认 `"0"`→False）、`REGISTER_DEFAULT_GROUP`（默认 `DEFAULT_GROUP_NAME`）。
  - 前端 `backend/frontend/src/api/auth.js`：新增 `register(username,password)`；`Login.vue`：登录/注册模式切换 + 注册表单（用户名/密码/确认密码）+ 成功后切回登录并预填。
  - 根目录 `.env.example`：补充 `ALLOW_REGISTRATION=0` 与 `REGISTER_DEFAULT_GROUP` 说明（注意：compose 读的 `.env` 是**项目根目录**，非 `backend/`）。
- 构建期 bug（已修）：`config.py` 第40行 `REGISTER_DEFAULT_GROUP = os.getenv("REGISTER_DEFAULT_GROUP", DEFAULT_GROUP_NAME)` 在 `DEFAULT_GROUP_NAME` 定义（第43行）之前引用 → 容器启动 `NameError` 崩溃。修复：把 `DEFAULT_GROUP_NAME` 定义提前到使用之前。改后 `py_compile` 通过 + 重建。
- 重建：`bash backend/offline/build_image.sh`，新镜像 `password-manager:latest` = `1816d3a0163d`（219MB）；`backend/offline/password_manager_image.tar` 更新（216M）。
- 端到端验证（临时容器 `pm-reg` `ALLOW_REGISTRATION=1` + `pm-reg2` 默认关）：
  - 开关关：`POST /api/auth/register` → 403「当前系统未开放注册」✅
  - 开关开：注册新用户→200；重复用户名→400；弱密码→400；确认不一致→400；注册用户 SCRAM 登录→200 拿到 token；`/api/auth/me`→`is_admin=false, groups=[{"id":1,"name":"默认分组"}]` ✅
  - 验证后两容器与 `.reg_data*` 清理。
- 排错经验（重要）：前端 SCRAM 必须用 `scramProof(password, saltHex, nonceHex)`（走 `sm3Bytes` 字节级拼接）；**误用 `sm3Hex` 会导致 proof 错、登录 401**（`sm3Hex` 把入参当 UTF-8 字符串编码，非原始字节）。E2E 测试用 `scramProof` 即可复现真实浏览器登录。
- 交付物：镜像 `1816d3a0163d` + `password_manager_image.tar`（216M）。

## 注册按钮按开关显隐（ALLOW_REGISTRATION=0 不展示）
- 需求：配置 `ALLOW_REGISTRATION=0` 时，前端登录页不显示「注册」入口（原本无条件显示，点了才 403）。
- 改动：
  - `backend/app/routers/auth.py`：新增**公开**（无需登录）`GET /api/auth/register/status` → `{"allow_registration": bool(ALLOW_REGISTRATION)}`。
  - `backend/frontend/src/api/auth.js`：新增 `registerStatus()`。
  - `backend/frontend/src/components/Login.vue`：`onMounted` 调 `registerStatus()`，`allowRegister` ref 默认 false（安全姿态：宁可隐藏也不展示必然 403 的入口）；仅 `allowRegister` 为真时显示登录页「还没有账号？注册」链接（`v-if="allowRegister"`）。
- 重建：`bash backend/offline/build_image.sh`，新镜像 `password-manager:latest` = `756126d83e96`（219MB）；`backend/offline/password_manager_image.tar` 更新（216M）。
- 验证（临时容器）：`ALLOW_REGISTRATION=1` → status 返回 `{"allow_registration":true}`；默认（关）→ `{"allow_registration":false}`；新 bundle `index-2OC9u2Pu.js` 含 `register/status` 探测逻辑。验证后两容器与 `.on_data/.off_data` 清理。
- 交付物：镜像 `756126d83e96` + `password_manager_image.tar`（216M）。

## 新增「授权管理」页面（逐用户操作权限，后端强制拦截）
- 需求：在「系统管理用户操作」下新增授权管理页（仅管理员可见）；页面列出当前系统所有操作按钮，勾选即授权，用户只能用勾选的操作。
- 权限模型（授权即限制，与历史约定一致）：
  - 无记录 → 全部可用（存量用户不被锁死）；有记录 → 仅清单内可用；`is_admin` 永远全开。
  - 前端按钮 `can(key)` 显隐只是辅助，**真正拦截在后端 `require_perm` 依赖（API 层 403）**。
- 后端改动（全部 `py_compile` 通过）：
  - `backend/app/perms.py`（新）：`PERMISSION_CATALOG`（15 个键，按 密码库/密钥库/账户/系统管理 分组）、`require_perm(key)` 依赖、`get/set/delete_user_permissions` 助手；`ALL_PERM_KEYS` 校验写入防脏数据。
  - `backend/app/models.py`：新增 `UserPermission(user_id PK, perms Text JSON)`，随 `create_all` 自动建表。
  - `backend/app/core/deps.py`：新增 `is_global_admin(db,user)` 与 `require_global_admin`（超管校验）。
  - `backend/app/routers/auth.py`：`/me` 返回 `permissions`+`is_global_admin`；新增公开 `GET /api/auth/permissions/catalog`（超管可见全目录）；`change-password/begin` 与 `/verify` 均挂 `Depends(require_perm("account.change_password"))`（**注意：begin 一开始漏挂，已补**，否则收回改密权限仍能发起挑战）。
  - `backend/app/routers/permissions.py`（新，超管专属）：`GET/PUT/DELETE /api/admin/permissions/users/{uid}`（PUT 覆盖写入允许清单；DELETE 恢复全开）。
  - `backend/app/routers/passwords.py`：9 个端点挂 `require_perm`（create/edit/delete/batch_delete/import/export/view）；`/template` 复用 `pw.import`。
  - `backend/app/routers/keys.py`：4 个端点挂 `require_perm`（generate/import/delete/batch_delete）；**密钥库仍 `require_admin` 层，per-user 授权为 no-op**（与 catalog「系统管理」注释一致，设计约定）。
  - `backend/app/routers/admin.py`：`sys.user_manage`/`sys.group_manage`/`sys.audit_view` 挂依赖（逐用户授权对其不生效，仅展示）。
  - `backend/app/main.py`：`include_router(permissions.router)`。
- 前端改动（已编入 dist，产物 `index-cOJjCPJ-.js` 首版 / 经 begin 修复后重建）：
  - `store.js`：`state.permissions` + `refreshMe` 写入 + 导出 `can(key)`（无记录/管理员→true）。
  - `api/auth.js`：新增 `permissionsCatalog()`/`getUserPermissions(uid)`/`setUserPermissions(uid,perms)`/`resetUserPermissions(uid)`。
  - `components/PermPanel.vue`（新，超管专属授权页）：用户下拉 + 分类勾选 + 全选/全不选/重置为全部可用/保存；管理员禁用不可限制；`onMounted` 拉 catalog，`res.permissions===null` 时全选。
  - `components/AdminModal.vue`：import PermPanel；subtabs 加「授权管理」（`v-if="state.isGlobalAdmin"` 仅超管可见）；`<PermPanel v-if="subtab==='perm'">`。
  - `App.vue`：顶栏「修改密码」按钮加 `v-if="can('account.change_password')"`。
  - `components/PasswordPanel.vue`：新增/导入/批量删除/导出/查看/编辑/删除/记录 按钮按 `can('pw.*')` 显隐（记录复用 `pw.view`）。
  - `KeyPanel.vue`：保持 `v-if="state.isAdmin"` 管理员专属（未改）。
- 目录键与 `require_perm` 实测**完全一致**（曾误以为不一致，实为 E2E 脚本写错预期键）：`pw.{create,edit,delete,batch_delete,import,export,view}`、`key.{generate,import,delete,batch_delete}`、`account.change_password`、`sys.{user_manage,group_manage,audit_view}`。
- 端到端验证（脚本 `backend/offline/e2e_perm_test.py`，临时容器 `pm-verify:9014`，SCRAM 登录）：
  - catalog 返回 200、含全部分组与 15 个键，且与各路由器 `require_perm` 键一一对应 ✅
  - 无记录普通用户：pw.create/view/unlock/export/account.change_password 全部可达（非 403）✅
  - 超管设「仅 pw.view」后：`/me` 正确回 `permissions:["pw.view"]`；已授权 pw.view→200；未授权 pw.create/import/account.change_password/export→403 ✅
  - 删除授权记录后 pw.create 恢复 200 ✅
  - ALL PERM E2E PASS ✅
- 重建：首版镜像 `e7b9c9e4af29`（14:55，缺 begin 守卫）→ 修复 auth.py 后 `docker cp` 进 `pm-verify` 验证通过 → **重建镜像含 begin 守卫**，后台跑 `bash backend/offline/build_image.sh`（任务 88NH8s），完成后更新 `password_manager_image.tar`。
- 交付物：镜像 `password-manager:latest` + `backend/offline/password_manager_image.tar`。

## 需求：登录账号 1 分钟无操作自动取消登录态（问题 1）
- 实现：前端空闲监听（JWT 单页应用，零后端依赖，最稳妥）。
- `backend/frontend/src/store.js`：新增空闲超时模块
  - `IDLE_TIMEOUT_MS = 60*1000`（1 分钟，命名常量易改）、`IDLE_RESET_THROTTLE_MS=3000`（活动重置节流，避免 mousemove 高频抖动）。
  - `onActivity()` 监听 `mousemove/mousedown/keydown/touchstart/scroll/click/wheel`，刷新倒计时；`triggerIdleLogout()` 调 `doLogout()` + `showToast('由于 1 分钟无操作，已自动退出登录', true)`。
  - `startIdleMonitor()` 幂等（全局监听只绑一次），每次调用重置 60s 倒计时；无 token 时不生效。
- `backend/frontend/src/App.vue`：`onMounted` 在 `bootstrap()` 成功恢复会话后调 `startIdleMonitor()`；`onLoggedIn()` 登录成功时也调（重置窗口）。`loggedIn=!!state.token` 驱动，登出即自动回登录页。
- 注意：这是客户端登出，JWT 在服务端仍按其有效期有效；对「界面上取消登录状态」需求已满足。如需服务端强制失效（token 作废）需另做会话表/短时效+刷新，当前未做（待用户确认是否需要）。
- 状态：代码完成 + 本地 `vite build` 通过（产物 `index-CEbGkoK6.js`）。**尚未进镜像**：与授权功能 begin 守卫修复同待统一重建（用户可能还有问题 2/3，计划汇总后一次性 `bash backend/offline/build_image.sh`）。

## 服务端强制失效（问题 1 的「服务端也强制失效」补充）
- 需求：空闲登出不仅前端清状态，令牌在服务端也必须作废（被截获也无法再用）。
- 方案：JWT 嵌入 `jti` + `auth_sessions` 表（服务端会话），`get_current_user` 校验会话有效性。
- 后端改动（全部 py_compile 通过）：
  - `backend/app/config.py`：新增 `SESSION_IDLE_SECONDS=60`（默认 60s，与环境变量 SESSION_IDLE_SECONDS 对齐前端空闲）。
  - `backend/app/models.py`：新增 `AuthSession(jti PK, user_id, created_at, last_activity, revoked)`（epoch 秒存整数，避免时区坑）。`init_db()` 的 `create_all` 自动建表。
  - `backend/app/security.py`：`create_token(username, jti)` 现在把 `jti` 写入 payload。
  - `backend/app/sessions.py`（新）：`new_jti()` / `create_session(db,user_id,jti)` / `revoke_session(db,jti)` / `is_session_valid(db,jti)`（无 jti/不存在/已吊销→False；`now-last_activity>SESSION_IDLE_SECONDS`→吊销并返回 False；否则节流刷新 last_activity 返回 True）。
  - `backend/app/core/deps.py`：`get_current_user` 解码出 `jti` 后先 `is_session_valid(db,jti)`，无效则 401「登录已失效，请重新登录」（JWT 未过期也失效）。
  - `backend/app/routers/auth.py`：`/login` 与 `/login/verify` 登录成功时 `new_jti()`+`create_session(db,user.id,jti)` 再签发带 jti 的 token；新增 `POST /api/auth/logout`（取当前令牌 jti 调 `revoke_session` 吊销）。
- 前端改动：
  - `backend/frontend/src/api/auth.js`：新增 `logout()` → `POST /api/auth/logout`（失败忽略，best-effort）。
  - `backend/frontend/src/store.js`：`doLogout()` 先 `if(state.token) apiLogout()` 吊销服务端会话，再清本地状态；`api()` 在发起 fetch 时同步读 localStorage token，顺序正确（吊销请求带当前 token）。
  - 手动退出 + 空闲自动登出 都会触发 `doLogout()` → 服务端吊销。
- 迁移注意：旧令牌（无 jti）在升级后一律 401→强制重新登录（符合「服务端强制失效」预期，更安全）。
- 验证脚本 `backend/offline/e2e_session_test.py`：MODE=logout（登出后同令牌 401、重登可用）/ MODE=idle（容器 SESSION_IDLE_SECONDS=2，静置 3s 后 401）。
- 统一重建镜像（任务 Z0iCGe，含 begin 守卫 + 前端空闲登出 + 服务端会话吊销 + logout 修复）：构建完成（镜像 `9b8832333715`，16:45）→ 起 pm-verify(9014)/pm-idle(9015) 验证：perm E2E 全 ✅、session idle 模式（9015，SESSION_IDLE_SECONDS=2）全 ✅。

## BUG 修复：/api/auth/logout 未能吊销会话（服务端强制失效失效）
- 现象：E2E 验证「登出吊销」失败——`POST /api/auth/logout` 返回 200，但同令牌 `/api/auth/me` 仍 200（应 401）；而「空闲超时吊销」正常（走 `is_session_valid` 内部 revoke）。
- 根因：`auth.py` 的 `logout` 端点调用 `decode_token(...)` 取 jti，但 `from ..security import (...)` **未导入 `decode_token`**；该调用被外层 `try/except Exception` 静默吞掉 → jti=None → `revoke_session(db,None)` 直接 return → 会话从未吊销。`py_compile` 查不出（仅语法），运行时 NameError 被吞。
- 教训：**依赖函数未导入却被 try/except 包裹会静默失败**。登出/吊销类接口必须显式验证「吊销后确实失效」，不能只看返回 200。
- 修复：① import 补 `decode_token`；② `logout` 改用 `HTTPBearer`（`creds: HTTPAuthorizationCredentials = Depends(bearer_scheme)`）取 token，去掉掩盖错误的 try/except（decode 失败应 500 而非静默放行）；③ 顶部加 `from fastapi.security import HTTPAuthorizationCredentials, HTTPBearer` 与 `bearer_scheme = HTTPBearer()`。
- 验证：`docker cp` 修复后进 pm-verify 复验 MODE=logout 全 ✅（登录后 200 / logout 200 / 登出后 401 / 重登 200）。
- 状态：修复已落盘（auth.py）；最终重建（任务 Z0iCGe，BUILD_EXIT=0，2m29s）完成，镜像 `a9899ddc59f6` + `password_manager_image.tar` 已更新（17:11）。新镜像起容器跑全量 E2E：**perm 全✅ / session logout 全✅ / session idle(2s) 全✅**。验证容器(pm-verify/pm-idle/pm-final-1/2)与数据卷已清理，交付 `backend/offline/password_manager_image.tar` + 根目录 `docker-compose.yml`。
