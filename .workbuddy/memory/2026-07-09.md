# 2026-07-09 工作日志

## 五大功能 E2E 验证 + 镜像重建
- 在 live 容器 `pm-test2`(9012) 上完成 5 大功能的端到端 HTTP 验证（加密在服务端容器内，本机 HTTP 客户端安全，绕开沙箱强杀）：
  - 功能4 SCRAM-SM3 登录：`e2e_scram.js`(Node) 5 项全过（begin→T=SM3(pw||salt)→proof=SM3(T||nonce)，错密 401，旧明文 /login 仍可用）。**关键坑**：客户端拼接必须用原始字节，不能把 hex 字符串当 UTF-8 再哈希。
  - 功能5 批量用户：`e2e_http_test.py` —— xlsx/csv 模板下载 200；CSV 上传（multipart）3 行=2 建+1 重名错；xlsx 模板回环可解析。
  - 功能3 GPG 口令：`e2e_extra.py` —— 容器内 `key.protect` 生成受口令 GPG 私钥，导入带口令成功、错口令返 400(`Passphrase was incorrect!`)；用该密钥加密密码→unlock 成功。
- **pgpy API 坑**：正确为 `PubKeyAlgorithm.RSAEncryptOrSign` / `KeyFlags` / `str(key.pubkey)` 取公钥；`key.protect(pass, AES256, SHA256)` 上口令；import 前 `sys.modules['imghdr']=types.ModuleType('imghdr')`。
- index.html 缓存版本 `?v=5`→`?v=6`。
- **镜像重建（docker commit 增量，绕开 `--no-cache` SIGKILL）**：起 `pm-build sleep 600` → `rm -rf /app/app` → `docker cp backend/app`+`run.py` → 清 `*.pyc` → `commit --change 'CMD ["python","run.py"]' --change 'EXPOSE 9010' --change 'VOLUME ["/app/data"]'` → `docker save` 到 `backend/offline/password_manager_image.tar`(231MB，较旧 425MB 大幅瘦身)。新镜像起独立容器 `pm-smoke`(9099) 验证：SCRAM 登录/keys/status/模板全 200。
- 清理：删 `backend/node_modules`(失败 puppeteer 残留)、`offline/_*` 临时测试；保留 `e2e_http_test.py`/`e2e_extra.py` 作为回归脚本。
- 已更新 `MEMORY.md`（SCRAM 协议、GPG 口令工作流、pgpy API 坑、5 功能落点、commit 增量法）。
