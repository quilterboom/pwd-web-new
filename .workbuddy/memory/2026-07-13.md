# 2026-07-13 工作日志

## 重建镜像（Vue 重构后首次打包）
- 用户已将前端重写为 **Vue3 + Vite**（`backend/frontend/`，20+ 个 `.vue` 组件；AdminModal/ChangePwModal/UserFormModal/ImportModal/ExportModal 等）。`backend/app/static` 已删除，前端产物改为 `backend/frontend/dist/`，由 `main.py` 的 `NoCacheStaticFiles(directory=STATIC_DIR="/app/app/static", html=True)` 同源托管。
- 构建流程：`cd backend/frontend && npm install && npm run build`（用托管 node 22.22.2）→ 产出 `dist/index.html` + `assets/index-*.js|css`（content-hash 文件名，外链、无内联脚本，CSP `script-src 'self'` 兼容）。
- 重建镜像沿用「commit 增量法」：`pm-build`（从 password-manager:latest）→ `rm -rf /app/app` → `docker cp backend/app` → `docker cp backend/frontend/dist /app/app/static` → 清 pyc → `docker commit --change 'ENTRYPOINT ["python","run.py"]' ...` → `docker save` → `password_manager_image.tar`（**232MB**）。**注意前端必须额外 cp 到 `/app/app/static`**，否则容器内无静态页（旧 commit 法只 cp backend/app 会漏掉前端）。
- 验证：全新容器（干净卷）启动 uvicorn 正常；`GET /` 200（Vue index）、`/assets/*.js|css` 200；`/api/auth/login/begin` 返 SCRAM salt/nonce。E2E：`e2e_import_test.py` 全绿；`e2e_changepw_test.py` 前 5 步通过（第 6 步因登录限流 429 中断，非后端 bug）。

## 本次发现的后端接口变更（用户代码）
- **`POST /api/passwords/import` 新增必填 `group_id: int = Form(...)`**：导入必须选定目标分组（不再按表格「所属分组」列推断）。旧 E2E 未传 → 422；已修正脚本补 `group_id`。
- **导入模板废弃 csv**：`GET /api/passwords/template?fmt=csv` 现返 400（"已废弃 csv"），仅 xlsx 可用。
- **新增登录限流**：`routers/auth.py` 的 `_login_rate_limit(request)` 加到 login/begin、login/verify、change-password/begin、change-password/verify 全部端点；频繁尝试 → 429「登录尝试过于频繁」。E2E 连续登录会触发，测试间需间隔或忽略。
- 后端 `routers` 重组：`admin.py` 拆为 `mine_router/users_router/groups_router/audit_router`；`main.py` 已相应 include。

## 注意
- `pm-test2` 旧容器曾绑定挂载 `backend/app:/app/app`（前端迁出后 `/app/app/static` 缺失 → `GET /` 404）。已重建 pm-test2 为**直接从新镜像起、不绑定挂载**、干净数据卷、9012→9010，供 Vue 预览。后续前端改 `backend/frontend/src/*` 后需 `npm run build`，容器不会热更新（除非重新打包/commit）。
- `build_image.sh` 是官方构建路径（`npm build` + `docker build --platform linux/amd64`），与本次 commit 法产物等价；联网构建走 PyPI。

## 改用标准 docker build 重新构建（用户要求看到真正的 docker 构建）
- 用户反馈之前用 `docker commit` 增量法「看不到构建」，本次按要求改用官方 `bash backend/offline/build_image.sh`（联网 PyPI）。完整流程日志可见：`npm install` → `vite build`（36 modules，产物 `assets/index-Dj8wMm3E.js` 137KB / `index-MnJTo4m9.css` 15.5KB）→ `docker build --no-cache --platform linux/amd64`（python:3.13-slim + pip 装 fastapi0.139/uvicorn0.50/PGPy0.6/gmssl3.2.2/sqlalchemy2.0.51 等）→ `docker save`。
- **新镜像 `password-manager:latest` = `c01690bfa28d`，219MB**（比 commit 法的 232MB 更小更干净）；`backend/offline/password_manager_image.tar` 已更新（~215MB，时间戳 08:41）。
- 验证（临时容器 pm-buildcheck @ 9015，干净卷，ADMIN_PASSWORD=TestPass!2026）：`GET /` 200 且服务 Vue index.html + 新 hash 静态资源均 200；`e2e_import_test.py` **ALL PASS**（含解密 SecretA!1、xlsx 回环导入）；`e2e_changepw_test.py` 前 5 步通过，第 6 步 admin 改密命中 429（登录限流按设计生效，非故障）。验证后已清理临时容器/数据。

## 字段改造三需求 + 验证（密码文件名称/系统/用户名、导出放开、筛选框）
- 需求来源：用户要求 (1) 导出按钮普通用户也能用（去管理员门禁）；(2) 新增密码页「名称」改「密码文件名称」，新增「用户名」「系统」输入框，列表/导入模板/导出模板字段同步；(3) 密码页新增「分组」「系统」筛选框。
- 后端落实（前序会话已完成，本次复核+验证）：`models.PasswordEntry` 新增 `system` 列（复用空置 `title` 作密码文件名称，前端必填）；`routers/passwords.py` 的 `CreateRequest/UpdateRequest` 加 `system`，`list_passwords` 加 `group_id`/`system` 过滤，`_serialize_meta`/模板/导出表头同步新字段；`/export` 端点 `require_admin`→`get_current_user`（Req1）；前端 `PasswordFormModal/PasswordPanel/ViewModal/ExportModal/AdminModal/HistoryModal/utils.js` 同步标签与列。
- **发现的回归 bug（已修）**：前序 `replace_all` 把 `system=entry.system,` 注入到 `History(...)` 审计构造器，但 `History` 模型无 `system` 列 → 新增密码 500。修复：`models.History` 增加 `system` 列（`String(255)`，与 PasswordEntry.system 对应，用于审计）；DB 自动迁移 `_migrate_columns()` 会补齐，全新库直接建表。审计 comment 仍用 `changes.append("system")`，前端 `HISTORY_FIELD_LABELS` 已含 `system:'系统'`。
- **重建镜像（含 History.system 修复）**：`bash backend/offline/build_image.sh` → 前端 `index-DeVG2r0M.js` 138.84KB；镜像 `password-manager:latest` = **`7a5816d2491c`**（13:33，219MB）；`backend/offline/password_manager_image.tar` 更新（216M，13:33）。
- **全量验证通过（临时容器 pm-buildcheck @ 9015 干净卷）**：
  - `verify_fields.py`（容器内 BASE=localhost:9010）：新增带 密码文件名称/系统/用户名 条目→200；system 过滤命中且字段正确；group_id 过滤命中；模板表头含[密码文件名称/系统/用户名/密码明文/备注]；导出表头含[密码文件名称/系统/用户名/加密方式/所属分组/密钥/密码明文/备注/更新时间]且数据行正确；**Req1 非管理员导出→200**。`ALL FIELD VERIFY PASS`。
  - `e2e_import_test.py`：**ALL E2E PASS**——含旧表头(标题/账号)xlsx 批量导入→2 创建、解密 SecretA!1、xlsx 回环导入→2 创建、csv 模板 400（已废弃）。
  - `e2e_changepw_test.py`（放宽 LOGIN_RATE_LIMIT=1000 跑，否则连续登录触发 429）：**ALL CHANGE-PW E2E PASS**（非管理员自助改密、新密码登录、错误当前密码401、过短400、管理员自助改密+还原）。
- **E2E 测试脚本改动（保留为回归用例）**：
  - `e2e_import_test.py`：去掉对 `docker exec pm-test2` 的依赖——xlsx 改为测试进程内用 `openpyxl` 直接生成（`from openpyxl import Workbook; from openpyxl.styles import Font`）；`BASE` 改 `os.getenv("BASE", "http://localhost:9012")`；**Req1 断言改写**：原「非管理员导出应 403」改为「非管理员导出可达（不再 403）」，`assert st != 403`。
  - `e2e_changepw_test.py`：`BASE` 改 `os.getenv("BASE", ...)`。
  - 运行方式（容器内）：`docker cp` 进容器，`docker exec -e BASE=http://localhost:9010 python /e2e_*.py`。
- 验证后已清理临时容器 pm-buildcheck 与 `.verifydata` 数据卷。最终交付物：新镜像 `7a5816d2491c` + `password_manager_image.tar`。

## 空态文案 bug 修复（2026-07-13 下午）
- 现象：密码页「暂无密码记录，点击右上角「新增密码」开始。」**有数据时也展示**。根因：该 banner 原用 `v-if="!state.entries.length"`，而 `state.entries` 是 store 里 `loadEntries()` 取的**全量快照**，与密码页实际驱动表格的**分页结果**（`fetchEntries()` 写入的 `entries`/`entriesTotal`）是两套数据源，易因刷新时序/筛选导致 `state.entries` 为空而表格有数据 → banner 误显；同时「无匹配结果」行也误用了 `state.entries.length`。
- 修复（`PasswordPanel.vue`）：空态完全改由**分页结果**驱动——新增 `loaded`(首次拉取完成)`、`hasFilter`(分组/系统/关键字任一非空) 两个 computed/ref；banner 改 `v-if="loaded && entriesTotal === 0 && !hasFilter"`；「无匹配结果」行改 `v-if="loaded && !entries.length && (entriesTotal > 0 || hasFilter)"`。效果：有数据（含筛选后无结果）绝不再显示新增引导 banner。
- 前端 `npm run build` OK（新产物 `index-Du-WbS5x.js`）；已 `build_image.sh` 重建镜像 → 新镜像 `c7450c14d623`（219MB，amd64）+ `password_manager_image.tar`（216MB）。
- 验证：容器启动后 `GET /` 引用 `index-Du-WbS5x.js`，且编译产物中 banner 守卫确为 `loaded && entriesTotal===0 && !hasFilter`（`b.value&&p.value===0&&!M.value`），空态文案不再出现在服务端 HTML（客户端按条件渲染）。临时容器 pm-emptycheck 已清理。

## 批次18（批量删除功能 + 二次确认）
- 范围：密码页 + 密钥库（用户确认）。复用既有两步确认弹窗（DeleteConfirmModal：风险提示 → 键入「确认删除」），天然满足「二次确认」。
- 后端：
  - `routers/passwords.py` 新增 `POST /api/passwords/batch-delete`（`BatchDeleteRequest{ids:List[int]}`），软删(deleted=True)+逐条写 History 审计+`ensure_group_access` 校验；返回 `{deleted,skipped,requested}`（无权限/不存在计入 skipped 不中断）；置于 `DELETE /{pid}` 之前。
  - `routers/keys.py` 补 `from typing import List`；新增 `POST /api/orgkeys/batch-delete`（管理员 `require_admin`），硬删+审计；同样 skipped 逻辑。
- 前端 store：新增 `state.selectedKeyIds` 及 `isKeySelected/toggleKeySelect/setKeySelection/clearKeySelection`（与密码勾选隔离，防跨页串号）；新增 `requestBatchDelete(type,ids)`；`confirmDelete` 扩展批量分支（调对应 /batch-delete，清空选择，刷新列表）；`doLogout` 清空 selectedKeyIds。
- 面板 UI：PasswordPanel 工具栏加「🗑 批量删除(N)」（用 selectedIds）；KeyPanel（仅 state.isAdmin）加勾选列+全选+「批量删除」，表头/空行 colspan 随 isAdmin 调整（admin 9 列 / 非 admin 8 列）；DeleteConfirmModal 文本支持「N 个」。
- 构建：**build_log18（19:51 完成，EXIT=0，镜像 ???，tar 19:51:26）**。实证：`/tmp/verify_batch_delete.py` 起 `pm-verify6`@9024 ALL PASS ✅——密码 deleted=3/部分跳过 deleted=1 skipped=2、密钥 deleted=2、批量删除后均不在列表、审计命中 12 条「批量删除」记录。前端产物含「批量删除」(6处)+`batch-delete` 调用。清理容器+卷。
- 注意实证坑：`/api/groups` 404 → 用 `/api/groups/mine`；创建密码需 `entry_password` 字段；中文 q 参数 URL 需编码（已改拉全量本地过滤）。
- 最终交付物：`backend/offline/password_manager_image.tar`（~72MB，19:51）含密码/密钥库批量删除 + 二次确认。
- 20:10 改动：`styles.css` 的 `.container` 由 `max-width:1000px` 改为 `width:100%;max-width:100%`（main.container 主内容区铺满，保留上下24px/左右24px边距）。**build_log19（20:10 完成，EXIT=0，镜像 23f49db4955a，tar 20:10:56）**。容器 `pm-verify7`@9025 `GET / → 200`，产物 CSS 含 `.container{width:100%;max-width:100%;margin:24px auto;padding:0 24px}`。清理容器+卷。
